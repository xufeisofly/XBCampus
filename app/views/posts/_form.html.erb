<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
      <% post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :主题 %>
    <%= form.text_field :topic, id: :post_topic %>
  </div>

  <div class="field">
    <%= form.label :详情 %>
    <%= form.text_area :content, id: :post_content %>
  </div>

  <div class="field">
    <%= form.label :联系人 %>
    <%= form.text_field :contact_person, id: :post_contact_person %>
  </div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
