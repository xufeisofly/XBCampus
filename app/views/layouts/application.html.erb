<!DOCTYPE html>
<html>
  <head>
    <title>XBCampus</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>

    <% if content_for? :vue %>
      <%= yield(:vue) %>
    <% end %>
  </head>

  <body>

    <% if student_signed_in? %>
      Logged in as <strong><%= current_student.email %></strong>.
      <%= link_to 'profile', current_student %> |
      <%= link_to "Sign out", studentsignout_path, method: :delete %>
      <%= link_to "Settings", edit_user_path(current_student) %>
    <% else %>
      <%= link_to "Sign up now!", student_signup_path %> |
      <%= link_to "Sign in", studentsignin_path %>
    <% end %>

    <%= render 'shared/nav' %>
    <%= render 'shared/flash' %>
    <%= yield %>

    
    <div class="container">
      <%= debug(params) if Rails.env.development? %>
    </div>
  </body>
</html>
